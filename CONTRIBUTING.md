# 贡献指南

1. 先搜索现有 Issue 和公开项目，记录候选项目、维护状态、许可证与不采用原因，再开始较大功能。
2. 保持微信原生小程序与云开发边界，不把第三方 Key 或 AppSecret 放入前端。
3. 新功能需补充加载、空状态、失败重试、数据隔离与迁移测试。
4. AI 输出一律视为不可信输入，在云函数中做白名单、长度、数量和枚举校验。
5. 不提交真实配置、用户数据、日志导出或第三方仓库副本。
6. 除仓库安全脚本明确列出的两张视觉素材外，不提交图片、音视频、压缩包、数据库或其他二进制文件；需要新增公开素材时先审阅来源与隐私，再单独更新白名单和摘要测试。
7. 提交前按 `docs/RELEASE_CHECKLIST.md` 核对，并运行 `node scripts/validate.js` 和 `node scripts/check-staged-safety.js`。

首次克隆后运行 `git config core.hooksPath .githooks`，启用仓库提供的提交前安全检查。

提交信息采用 `feat:`, `fix:`, `docs:`, `test:`, `chore:` 等明确前缀。涉及 schema 或 AI 契约时，在 PR 中列出旧版本迁移证据。
