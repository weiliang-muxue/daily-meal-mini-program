# 安全政策

当前维护最新发布版本和唯一的 `v*` 版本 Branch；`main` 只在默认分支迁移期间作为过渡引用。

发现凭据泄露、越权访问、用户数据隔离失败、上传票据绕过或其他安全问题时，请通过 GitHub 仓库所有者公开资料中提供的私密联系方式报告；不要创建公开 Issue，也不要附带真实用户数据。报告中只提供最小复现步骤、受影响版本和脱敏证据。

项目不会要求贡献者提供任何 Key、Secret、部署口令、openid、健康记录或数据库副本。真实凭据只应由部署者本人写入微信云函数环境变量，并在疑似泄露时立即吊销和轮换。

本地提交钩子检查暂存索引，推送钩子和 GitHub Actions 检查完整提交范围、提交说明、Branch/Tag 名称及 annotated Tag 注释。仓库只允许 UTF-8 文本源码和两张经过路径、格式、大小及摘要校验的视觉素材；其他二进制、压缩包、日志、数据库和用户媒体一律拒绝。
