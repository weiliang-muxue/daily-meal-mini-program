# 变更日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。未发布内容先进入 `Unreleased`，发布时再归入明确版本。

## Unreleased

下一版本的用户可见变更记录在此。

- 用户状态升级为 schema v8，加入默认关闭的每日/周一至周五喝水提醒、起止时间和 30/45/60/90/120 分钟间隔；v1-v7 迁移不自动开启，也不改写既有餐单、历史、采购、健康或个人提醒。
- 保存喝水设置不申请权限；只有用户点击并二次确认后才逐条写入未来 30 天系统日历重复事项。拒绝、API 不支持和部分失败均明确提示，已写入事项需由用户在系统日历中删除。
- 当前视觉审计批次已按“先盘点、再统一修复、一次回归”完成：优先处理健康页视觉焦点、状态留白、字号层级、窄屏拥挤风险以及卡片/按钮/图形语言一致性；本轮仅调整表现层和对应门禁测试，不改变业务数据或 AI 传输契约。
- 本轮修改已通过完整门禁；候选提交 `45d4d0e` 包含健康页运动打卡焦点、状态留白、字号层级、窄屏约束及卡片/按钮语言收口，并已于 `2026-09-04` 以同一代码树重新预览、编译和上传微信开发版本（约 577.8 KB）。旧上传版本不代表本轮页面。

## [0.2.0] - 2026-08-27

状态：发布候选。本轮运行时对应提交 `45d4d0e` 已于 `2026-09-04` 完成微信开发版本上传；尚未设为体验版、提交微信审核或正式发布。上传、审核及正式发布完成前不创建 Tag；审核通过、仓库所有者最终确认并完成微信正式发布后，才在审核通过的完全相同 commit 上创建 annotated Tag。

### 新增

- 用户先选择餐次、任意 1–14 天周期（默认 1 天）、饮食目标、逐日运动安排、忌口与健康约束，再生成个人食谱。
- 餐次采用动态数组，支持早餐、午餐、晚餐、加餐任意组合；运动/不运动两套晚餐仅在用户主动选择时生成。
- AI 生成候选计划经服务端结构校验后预览，只有用户确认才替换当前计划。
- 每次新生成前单独展示第三方 AI 发送范围并要求用户主动勾选；修改生成条件会撤销旧同意，缺少当前同意协议的客户端、云函数或旧活动任务均禁止继续调用 AI。
- AI 生成的候选、当前餐单、采购清单和餐食编辑页统一显示“AI生成”标识；旧版迁移和个人计划不显示该标识。
- 从已确认餐次生成个人采购清单；AI 不猜测实时商品价格。
- 正式 AI URL、模型和协议作为非秘密版本配置；部署者只需在云函数环境变量填写 AI Key，密钥不进入客户端或 Git。
- AI provider 请求契约升级为 v9：运行时由部署者提供 HTTPS Responses 地址，使用 `gpt-5.6` 与 Responses API；云函数只发送标准 Bearer 鉴权和 JSON 内容头，移除 v8 的 provider 专用兼容头。兼容回退始终保留 `model`、`instructions`、`input` 和 `store:false`；旧 v8 云函数不会被新版客户端或发布探针误判为可用。
- AI 数据同意协议升级为 v2；服务商切换后旧活动任务会在调用新服务商前失败关闭，用户必须重新查看发送范围并主动勾选。已确认、候选和历史餐单、采购、健康及资料数据不会被重写或删除。
- AI 生成任务支持跨设备恢复、取消版本校验、断线续跑和终态并发保护；生成器版本独立记录为 `7`，新请求与新计划使用 `contractVersion: 2`。
- 慢速 AI 服务采用紧凑提纲和每次只生成 1 个餐位的详情分片，为固定 `gpt-5.6`、Responses 和 16000 输出 token 上限保留单请求时延余量；任意 1–14 天与动态餐次仍完整校验。详情分片按索引顺序生成，后续分片携带已完成餐名禁用清单；跨分片重复会在当前分片最多尝试 2 次，最终全计划仍执行严格去重。升级时生成器或契约版本不匹配的活动任务会明确失败关闭并要求重新生成，已确认计划保持不变。
- AI 任务以 task schema v3 保存本次独立同意版本和规范化当前/候选计划摘要。生成期间切换日期、晚餐模式、采购勾选、提醒、设置或餐次覆盖会与新候选安全合并；候选正文更新、确认、恢复、丢弃计划或生成偏好改变时，旧任务以冲突终结且不改写用户状态。
- AI 任务读取、领取、回写、取消、重放和维护路径均校验 task schema；缺失、非法或高于当前 v3 的版本会失败关闭或跳过，且不调用上游 AI、不写任务、分片、控制文档或用户状态。
- 缺少计划基线摘要的旧活动任务在重放、读取、领取、worker 回写和到期维护路径失败关闭；遗留正文和分片仍会清理，已确认计划、候选计划及用户状态 revision 保持不变。
- 新增计划预览、最近计划历史、确认与恢复流程；应用升级不会自动覆盖用户已确认的计划。
- 用户状态升级为 schema v7：生成偏好切换到 AI contract v2；计划历史明确保留最多 64 份，达到上限时显式拒绝且不静默淘汰，采购勾选和逐日晚餐模式按计划 ID 保存，切换或恢复旧计划时一并恢复。
- 旧 schema v1-v6 与旧版本机 pending 会增量迁移；v6 到 v7 只更新生成偏好的契约标记，不重写已确认、候选或历史餐单。历史 contract v1 餐单及 legacy contract v0 静态迁移餐单仍可查看、确认和恢复；离线提醒修改保留待重试，多设备 revision 冲突重放不会清空历史或串用计划 UI 状态。
- 本地缓存中的非法计划天数只会修复为默认 1 天；当前计划、候选、历史、个人餐次调整、采购勾选、提醒、设置和更新时间继续保留。云端非法天数仍严格拒绝，不放宽数据校验。
- 邀请制默认总容量为 4 人（1 位管理员加 3 个受邀成员）；邀请码单次使用并在创建 7 天后过期。
- 成员容量和邀请码有效期锁定为版本化业务规则，旧云端环境变量不会扩大人数或缩短有效期。
- 正式一次性邀请码升级为 32 位大写十六进制，明文只在创建时显示一次；管理员可查看脱敏的待使用邀请列表，并经过确认撤销邀请、立即释放对应名额。
- 受邀用户始终以普通成员加入，不能邀请或管理成员；资料页提供脱敏成员列表，唯一管理员必须显式选择接任者并经过两次确认，不进行自动提升。
- 资料页使用微信原生头像选择与昵称填写控件，不依赖已废弃的完整用户资料接口；头像和昵称均由用户主动操作并可随时修改。
- 用户可主动选择绑定手机号；完整号码和动态凭证不写入数据库、本地缓存或日志，核心餐单功能不要求绑定手机号。
- 头像和健康照片在云函数端按文件大小及真实图片文件头校验；替换后清理旧文件，删除账号仅移除当前身份的私人缓存。
- 私人数据清理会先把当前身份标记为不可写，再删除档案、餐单、采购、提醒、健康、图片、AI 任务和关联邀请并复查残留。普通成员同时退出；唯一且无其他成员的管理员改用全新随机引用恢复一条不含个人资料的最小管理员身份，避免已初始化实例永久失去管理入口。
- 个人餐次调整只保留仍存在于当前计划、候选计划或最近计划历史中的餐次 ID，避免多轮不同周期计划迭代积累失效数据。
- 首次管理员初始化改为云数据库控制台批准的一次性事务，不再把开发者工具调用来源当作管理员身份；八个云函数依赖固定并提供可复现锁文件。
- 手机页面补充 320/353/375/414 宽度、横屏、左右及底部安全区适配，改善多行输入聚焦、固定操作栏、健康月历和小屏触控热区。
- 健康页采用原创的活动环信息表达：未打卡、待保存/待更新、已打卡、待取消均使用独立图形、颜色与文字语义；只有云端已保存记录显示完整绿环。月历运动日同时显示浅绿整格、圆点和“运动”文字；周/月汇总环展示真实次数与分钟，不伪造目标完成率。
- AI 服务配置、鉴权、订阅组、模型或路由拒绝时，生成页明确显示“未完成”和管理员检查提示，不再提供无效的重复生成操作；当前已确认餐单保持不变。

### 文档

- 七个正式云函数统一显式声明 60 秒超时与 256 MB 内存；本地门禁校验候选源码，发布清单另要求部署者在完整部署后人工回查线上运行配置，避免把源码或部署成功提示误当作线上生效证据。

- 新增机器可检查的版本清单、升级策略、发布清单、支持、安全报告和发布后路线图说明。
- 补充 GitHub AI 食谱与微信云开发候选项目的更新时间、许可证、已覆盖能力和逐项适配差距。
- 补充公开 Issue、功能建议、私密安全报告及版本支持入口；公开反馈禁止附带密钥或真实个人数据。
- 提交安全检查覆盖全部 Git 索引和示例文件内容，拦截常见裸 Key、敏感变量赋值与私钥。
- 生成确认页在发送前明确显示 AI 服务域名和最小数据范围；微信身份、体重、照片、位置及历史记录不发送给 AI。

### 验证

- 修复公开仓库干净检出缺少 TabBar 运行时图标导致的 CI 失败；导航图标以精确哈希白名单纳入版本控制，安全历史扫描仅精确识别两个已审核的旧占位文件，Windows 与 Linux 换行均可执行颜色对比测试。
- 本次发布门禁覆盖 schema v8 增量迁移、AI contract v2 与 planner v7、任意 1–14 天动态餐次（默认 1 天）、按需双晚餐、邀请权限、十集合数据库安全规则精确匹配、AI 逐次同意与发送内容脱敏、健康月历、运动状态、可选喝水提醒、手机响应式及页面路由；当前候选的全量静态门禁已重新执行并通过。
- 本轮采用“完整测试盘点、冻结问题清单、统一批量修复、一次完整回归”的收敛方式；当前候选回归为 `validate` 通过（schema v8、13 路由、55 组测试）、微信自动化 `69/69`、AI 安全发布测试 `32/32`，交互回归 `23` 项通过、`5` 项按外部条件跳过、`0` 项失败，Console 阻断错误与异常均为 `0`。
- 运行时对应提交 `45d4d0e50256f3007245a7e12d5f4dd25ffdb053`（候选 Branch `refs/heads/v0.2.0`，tree `8368c87abf18429ffc86b993f38bb4c5ba1995ec`）已于 `2026-09-04` 重新完成预览、编译和微信开发版本上传，CLI 返回成功，上传包约 577.8 KB；上传前仅注入本机私有配置，未进入 Git。此前 `b06b3f0`、`5ce21a0`、`6468e9e` 与 `115cc5d` 的上传记录仅作为历史证据，不代表当前候选运行时。上传只产生开发版本，不等于体验版、审核或正式发布；体验版真机回归、线上五项云配置核对、提交审核和正式发布仍待完成。虚构 1 天条件的云端 AI 端到端候选预览已通过；10/14 天真实上游耗时尚未逐项测量，不能据 1 天结果外推。
- 视觉审计批次（提交 `45d4d0e`）已收紧健康月历与运动卡片的首屏节奏，压缩餐单/采购错误态的无效留白，统一全局卡片/控件阴影与标签圆角，强化 320–400px 日期和运动开关的稳定触控尺寸，并将月份导航改为 CSS chevron 以避免平台字体差异。对应专项测试和完整门禁已通过；随后以同一候选提交完成微信开发版本重新上传。

## 0.1.0 - 2026-08-25

### 新增

- 微信原生小程序基础版本：一周卡片、运动/不运动晚餐、采购清单、健康记录、私人照片和个人提醒。
- 微信云开发登录、邀请制成员、按 openid 隔离的持久化、schema v4 增量迁移和私人数据清理。
- 数据库拒绝客户端访问、一次性文件上传票据、提交前敏感信息检查。

### 已知限制

- 此版本仍以内置静态食谱作为运行时默认数据，后续动态 AI 计划版本将迁移为“无计划空状态”，但不会删除用户已经保存的云端数据。
